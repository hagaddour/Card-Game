#ifndef __PRINT_H__
#define __PRINT_H__

#include <string>
#include <vector>
using namespace std;

//used to print the display
//created so that we don't have to use a loop everytime we want to print the display
void print(std::vector<std::string> output);

#endif
